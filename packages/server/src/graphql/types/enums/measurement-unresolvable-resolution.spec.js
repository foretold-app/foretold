const index = require('./measurement-unresolvable-resolution');

describe('Measurement Unresolvable Resolution', () => {
  it('returns an object', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
