const { FeedItemChannel } = require('./feed-item-channel');

describe('Feed Item Channel', () => {
  it('returns a constructor', () => {
    expect(FeedItemChannel).toBeInstanceOf(Function);
  });
});
