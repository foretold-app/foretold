const agentMeasurables = require('./agent-measurables');

describe('Agent Measurables Resolvers', () => {
  it('class should be object', () => {
    expect(agentMeasurables).toBeInstanceOf(Object);
  });
});
