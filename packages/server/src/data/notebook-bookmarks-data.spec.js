const { NotebookBookmarksData } = require('./notebook-bookmarks-data');

describe('Notebook Bookmarks Data', () => {
  it('class should be a constructor', () => {
    expect(NotebookBookmarksData).toBeInstanceOf(Function);
  });
});
