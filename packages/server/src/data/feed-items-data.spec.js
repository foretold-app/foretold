const { FeedItemsData } = require('./feed-items-data');

describe('Feed Items Data', () => {
  it('class should be a constructor', () => {
    expect(FeedItemsData).toBeInstanceOf(Function);
  });
});
