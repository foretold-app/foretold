const { EmailEnvelopeTemplate } = require('./email-envelope-template');
const { FeedItemTemplate } = require('./feed-item-template');

module.exports = {
  EmailEnvelopeTemplate,
  FeedItemTemplate,
};
