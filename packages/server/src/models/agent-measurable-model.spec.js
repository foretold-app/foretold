const { AgentMeasurableModel } = require('./agent-measurable-model');

describe('Agent Measurable Model', () => {
  it('class should be a constructor', () => {
    expect(AgentMeasurableModel).toBeInstanceOf(Function);
  });
});
