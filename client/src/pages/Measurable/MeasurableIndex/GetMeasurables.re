open Foretold__GraphQL;

/* module GetMeasurables: SelectWithPaginationReducer2.Config = {
     type itemType = Context.Primary.Measurable.t;
     type callFnParam = string;
     let callFn = Queries.Measurables.component2;
     let isEqual = (a, b) => a == b;
     let hasId = (a: itemType, b) => a.id == b;
     let getId = (a: itemType) => a.id;
   };

   module Reducer = SelectWithPaginationReducer2.CreateReducer(GetMeasurables); */