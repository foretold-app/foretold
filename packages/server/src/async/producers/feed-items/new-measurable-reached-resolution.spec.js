const {
  NewMeasurableReachedResolution,
} = require('./new-measurable-reached-resolution');

describe('New Measurable Reached Resolution Feed Item Producer', () => {
  it('returns an constructor', () => {
    expect(NewMeasurableReachedResolution).toBeInstanceOf(Function);
  });
});
