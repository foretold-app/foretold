const { ChannelBookmarkModel } = require('./channel-bookmark-model');

describe('Channel Bookmark Model', () => {
  it('class should be a constructor', () => {
    expect(ChannelBookmarkModel).toBeInstanceOf(Function);
  });
});
