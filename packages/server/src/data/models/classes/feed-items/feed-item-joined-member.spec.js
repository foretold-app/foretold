const { FeedItemJoinedMember } = require('./feed-item-joined-member');

describe('Feed Item Joined Member', () => {
  it('returns a constructor', () => {
    expect(FeedItemJoinedMember).toBeInstanceOf(Function);
  });
});
