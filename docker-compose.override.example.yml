version: "2"

services:

  app:
    environment:
      - API_URL=<change-me>
      - AUTH0_SECRET=<change-me>
      - SLACK_WEBHOOK_URL=<change-me>
      - JWT_SECRET=<change-me>
      - BOT_TOKEN=<change-me>
      - BOT_AGENT_ID=<change-me>
      # SMTP
      - SMTP_HOST=smtp.host.com
      - SMTP_PORT=465
      - SMTP_USER=user@host.com
      - SMTP_PASS=pass
      - SMTP_FROM=User <user@host.com>

  database:
    environment:
      - POSTGRES_PASSWORD=<change-me>
