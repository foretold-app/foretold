const index = require('./member-invited-to-community');

describe('Member Invited to a Community Producer', () => {
  it('returns an object', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
