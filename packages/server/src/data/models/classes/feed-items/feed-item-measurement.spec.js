const { FeedItemMeasurement } = require('./feed-item-measurement');

describe('Feed Item Measurement', () => {
  it('returns a constructor', () => {
    expect(FeedItemMeasurement).toBeInstanceOf(Function);
  });
});
